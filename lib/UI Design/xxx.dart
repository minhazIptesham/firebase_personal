/*create() async {
  try {
    await firebase
        .collection("User Address")
        .doc(Email.text)
        .set({"Email": Email.text, "Password": Password.text});
  } catch (e) {
    print(e);
  }
}

update() async {
  try {
    firebase.collection("User Address").doc(Email.text).update({'Password': Password.text});
  } catch (e) {
    print(e);
  }
}

dispose() {
  Email.clear();
  Password.clear();
}*/ //Use for delete user data//




